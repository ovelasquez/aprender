{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typespublication</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typespublication.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ typespublication.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ typespublication.price }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typespublications_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typespublications_edit', { 'id': typespublication.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
